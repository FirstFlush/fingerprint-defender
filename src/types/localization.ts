import moment from "moment-timezone";
import { Localization } from "./environment";
import { DetectedLocalization } from "./detectedEnvironment";


export const spoofLocalization = (detectedLocalization: DetectedLocalization): Localization => {

    
} 